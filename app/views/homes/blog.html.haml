.blog.text_bk_layer.rounded_corner
  %h2.name
    = @blog.name
    %hr/
  %br/
  .body
    = raw sanitize(simple_format(@blog.body), :tags => %w(br p) )
  %hr/
  %hr/
  .see_all.right.white
    = link_to "See All", blogs_archive_path
